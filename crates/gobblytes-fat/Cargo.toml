[package]
name = "gobblytes-fat"
version = { workspace = true }
edition = { workspace = true }
rust-version = { workspace = true }
license = { workspace = true }
repository = { workspace = true }
description = "FAT32 filesystem adapter for gibblox block readers."
keywords = ["gibblox"]

[features]
default = []
std = []

[dependencies]
gibblox-core = { workspace = true }
gobblytes-core = { workspace = true }
hadris-fat = { workspace = true, features = ["alloc", "async", "read", "lfn"] }
# Direct constraints also pin hadris-fat's shared implementation dependencies
# for downstream consumers. A workspace lockfile alone cannot do that.
hadris-common = { workspace = true }
hadris-io = { workspace = true }
hadris-macros = { workspace = true }
tracing = { version = "0.1.41", default-features = false, features = ["attributes"] }

[dev-dependencies]
async-trait = "0.1.89"
futures = { version = "0.3.31", default-features = false, features = ["executor"] }
